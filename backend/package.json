{
  "name": "backend",
  "private": true,
  "version": "0.0.6",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only ./src/index.ts",
    "build": "esbuild ./src/index.ts --bundle --minify --platform=node --outdir=./dist && mkdir -p ./dist/logs",
    "start": "node ./dist/index.js",
    "type:check": "tsc --noEmit",
    "lint:fix": "eslint --fix .",
    "lint:check": "eslint .",
    "format:fix": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "body-parser": "^1.20.0",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.0.2",
    "express": "^4.18.1",
    "passport": "^0.6.0",
    "passport-azure-ad": "^4.3.4",
  },
  "devDependencies": {
  },
  "prettier": {
    "filepath": "../.prettierrc.json"
  }
}